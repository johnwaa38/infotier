const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); (async()=>{ await prisma.customerConfig.upsert({ where:{customerId:'cust_demo'}, update:{}, create:{ customerId:'cust_demo', approveThreshold:0.75, rejectThreshold:0.35, retentionDays:30, webhookUrl:'' } }); console.log('Seed complete'); process.exit(0) })().catch(e=>{ console.error(e); process.exit(1) })
